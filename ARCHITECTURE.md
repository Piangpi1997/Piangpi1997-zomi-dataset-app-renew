# Architecture
Import → Parse → Validate → Review → Store → Search/Filter → Export

Production extensions: PDF/Excel adapters, database persistence, authentication, role-based review, versioning, duplicate detection, AI-assisted translation/review, and server-side secrets.
