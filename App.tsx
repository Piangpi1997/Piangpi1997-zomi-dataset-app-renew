import{useMemo,useState}from'react';import{sampleDataset}from'./data/sampleDataset';import{categories,type DatasetRecord,type SourceStatus}from'./types/dataset';import{validateRecord}from'./services/validation';
export default function App(){const[rows,setRows]=useState(sampleDataset),[q,setQ]=useState(''),[cat,setCat]=useState('all'),[status,setStatus]=useState('all'),[open,setOpen]=useState(false);const[form,setForm]=useState<Omit<DatasetRecord,'id'>>({zomi:'',myanmar:'',english:'',category:'daily_conversation',dialect:'Zomi',source:'unverified'});const filtered=useMemo(()=>rows.filter(r=>Object.values(r).join(' ').toLowerCase().includes(q.toLowerCase())&&(cat==='all'||r.category===cat)&&(status==='all'||r.source===status)),[rows,q,cat,status]);const add=()=>{const e=validateRecord(form);if(e.length)return alert(e.join('\n'));setRows(v=>[{...form,id:`zomi-${Date.now()}`},...v]);setOpen(false)};const exportJson=()=>{const b=new Blob([JSON.stringify(rows,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='zomi-dataset.json';a.click()};return <main><header><div><small>ZOMI / TEDIM • DATA STUDIO</small><h1>Zomi Dataset Studio</h1><p>Clean, verify and prepare multilingual data for AI.</p></div><button className="primary" onClick={()=>setOpen(true)}>＋ Add record</button></header><section className="stats">{[['Total',rows.length],['Verified',rows.filter(x=>x.source==='verified').length],['Categories',new Set(rows.map(x=>x.category)).size],['Dialects',new Set(rows.map(x=>x.dialect)).size]].map(([a,b])=><div><b>{b}</b><span>{a}</span></div>)}</section><section className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Zomi, Myanmar, English..."/><select value={cat} onChange={e=>setCat(e.target.value)}><option value="all">All categories</option>{categories.map(x=><option>{x}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="all">All status</option><option>verified</option><option>unverified</option></select><button onClick={exportJson}>Export JSON</button></section><section className="card"><div className="head"><b>Dataset records</b><span>{filtered.length} shown</span></div><div className="scroll"><table><thead><tr><th>Zomi</th><th>Myanmar</th><th>English</th><th>Category</th><th>Dialect</th><th>Status</th></tr></thead><tbody>{filtered.map(r=><tr><td>{r.zomi}</td><td>{r.myanmar}</td><td>{r.english}</td><td>{r.category}</td><td>{r.dialect}</td><td><i className={r.source}>{r.source}</i></td></tr>)}</tbody></table></div></section>{open&&<div className="modal"><div className="modalbox"><h2>Add dataset record</h2>{(['zomi','myanmar','english','category','dialect']as const).map(k=><label>{k}<input value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label>source<select value={form.source} onChange={e=>setForm({...form,source:e.target.value as SourceStatus})}><option>verified</option><option>unverified</option></select></label><div className="actions"><button onClick={()=>setOpen(false)}>Cancel</button><button className="primary" onClick={add}>Save</button></div></div></div>}</main>}
